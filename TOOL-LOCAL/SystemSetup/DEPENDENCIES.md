# Dependency Setup — 2026-09-26

- Qwen: dùng nguyên `VietsubTranslationApprovedComponents.Qwen3_4B_Q4Km`, URI revision/size/SHA-256/license hiện có. Không có bản checksum thứ hai trong catalog Setup.
- Piper ONNX/config và uv: dùng các artifact đã pin trong `VietsubVoiceComponentStore`; provenance/license hiện hành ở `third_party/voice` được giữ nguyên.
- Piper offline-v3: bundle chứa CPython **3.11.15 x64**, uv **0.12.3**, bảy wheel Windows và model/config VAIS-1000. Builder dùng source pin và lockfile; `PIPER_OFFLINE_APPROVED.json` được nhúng vào ứng dụng để neo hash ZIP/manifest. Không dùng Python trong PATH hoặc registry. `.venv` được tạo ở thư mục cuối trên máy đích; không mang venv/marker từ máy build sang.
- Bộ Visual C++ x64 **14.50.35719.0** đi cùng Python: bốn DLL được lấy từ CAB của installer Microsoft đã pin SHA-256, không chạy installer/MSI. Worker xác minh các DLL thực sự được nạp từ payload; xem `third_party/voice/MSVC-NOTICE.md` và điều kiện phát hành liên quan.
- Piper và toàn bộ dependency: `piper-requirements.in` và `piper-requirements.lock`, resolve Windows/CPython 3.11.15 bằng uv đã pin, nguồn `https://pypi.org/simple`, chỉ wheel, `--require-hashes`. Worker kiểm Python và các phiên bản thực cài trước khi nạp model. Marker ràng buộc hash worker/requirements/Python/model/config và máy/tài khoản Windows.
- Không đọc cấu hình uv/pip của người dùng; loại biến môi trường UV/PIP/PYTHON kế thừa trước khi chạy uv và đặt cache riêng. Luồng mặc định dùng `--offline --no-python-downloads --no-index --find-links` và Python trong payload. Nhánh online cũ chỉ còn qua lựa chọn code tường minh phục vụ tương thích/test; không có fallback mạng tự động. Kokoro vẫn là gói tùy chọn riêng.
- OCR/media: runtime/model và FFmpeg đi cùng bundle. Setup kiểm OCR bằng ảnh raster Anh/Trung, kiểm manifest FFmpeg rồi tạo/probe WAV mẫu.
- OCR có bộ Visual C++ x64 riêng cạnh EXE: `msvcp140.dll`, `vcruntime140.dll`, `vcruntime140_1.dll`, `vcomp140.dll`, phiên bản **14.50.35719.0** từ installer Microsoft đã pin. Không dùng DLL trong payload Python để thay cho dependency của tiến trình desktop. `third_party/ocr/MSVC_RUNTIME.json` được nhúng vào assembly; startup kiểm đủ file/size/hash trước khi nạp OCR, báo riêng DLL thiếu/hỏng và Media Foundation của Windows thiếu. Build/publish bắt buộc có bundle trong `artifacts/ocr-native-runtime/msvc-14.50.35719.0-win-x64` (hoặc `OcrNativeRuntimeDirectory`). Chuẩn bị bằng `scripts/Prepare-OcrNativeRuntime.ps1 -InstallerPath <installer đã pin>`; script chỉ trích CAB, không chạy installer/MSI hoặc đổi Windows.
- `--check-bundled-components` chỉ đạt khi OCR nhận dạng fixture thật và bốn DLL Visual C++ được nạp từ thư mục ứng dụng. PATH chỉ chứa Windows không đủ chứng minh khả năng chạy độc lập, vì System32 vẫn có thể chứa Visual C++ của máy phát triển. Media Foundation là prerequisite của Windows; không copy DLL hệ thống vào ZIP.
- WebView2: kiểm trước giao diện; nếu thiếu, hộp thoại WinForms dẫn đến trang Microsoft và cho kiểm tra lại sau cài Evergreen x64. Không tải/chạy bootstrapper không có checksum/signature đã duyệt.

Tài liệu chính thức đã đối chiếu: [uv require-hashes](https://docs.astral.sh/uv/reference/cli/), [nguồn Python của uv](https://docs.astral.sh/uv/concepts/python-versions/), [Microsoft WebView2 distribution](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/distribution).

Đây là khóa kỹ thuật để kiểm thử, không thay thế phê duyệt phân phối thư viện. Hồ sơ FFmpeg vẫn có scope Development; cần nghiệm thu Windows sạch, license/provenance và phát hành theo quy trình trước khi gọi bộ cài là release-ready.
